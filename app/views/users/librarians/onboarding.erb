

<center>

    <h1>Congratulations <%=@user.username%> for becoming a Librarian of the NJ library system.</h1>
    <p>We just have a few more questions to get you fully onboarded.<p>

      <form action="/users/onboarding" method="POST">

               <label>What is your full name?</label>
                  <input type="text" name="user[name]" id="name" placeholder="">

              <br></br>

              <label>What is your age?</label>
                  <input type="text" name="user[age]" id="age" placeholder="">

              <br></br>

              <label>What year did you start working with us?</label>
              <input type="text" name="user[start_year]" id="start_year" placeholder="">

              <br></br>

                <p>What Libraries do you belong to?</p>
                <% Library.all.each do |library| %>
                <label><%= library.name %></label>
                <input type="radio" name="user[library_id]" value="<%= library.id %>" id="<%= library.name %>" <%='checked' if @user.library == library %>>
                <% end %>
                <br></br>
                <input type="submit" name="submit" value="submit">

      </form>

      <br></br>
      <b>
          <% if flash.has?(:message) %>
            <%= flash[:message] %>
          <% end %>
      </b>

</center>
